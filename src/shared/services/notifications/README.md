# NotificationService

Низкоуровневый сервис для работы с Web Notifications API.

## 📍 Назначение

Чистая обёртка над браузерным Notifications API без бизнес-логики.
Используется `NotificationScheduler` для показа уведомлений.

---

## 🎯 Архитектурная роль

```
┌─────────────────────────────────────┐
│   habitNotificationScheduler.ts     │ ← Habits-specific логика
│   (module-level)                    │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│   NotificationScheduler.ts          │ ← Мультимодульное планирование
│   (shared/services)                 │    + группировка
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│   NotificationService.ts            │ ← Низкоуровневая работа с API
│   (shared/services)                 │    (разрешения + показ)
└─────────────────┬───────────────────┘
                  │
                  ▼
        [ Web Notifications API ]
```

---

## 📦 API

### Методы

#### `isSupported(): boolean`
Проверка поддержки Web Notifications API.

```typescript
if (NotificationService.isSupported()) {
  // API доступен
}
```

#### `getPermissionStatus(): NotificationPermission`
Получение текущего статуса разрешения (`'granted' | 'denied' | 'default'`).

```typescript
const status = NotificationService.getPermissionStatus();
```

#### `requestPermission(): Promise<NotificationPermission>`
Запрос разрешения на отправку уведомлений.

```typescript
const permission = await NotificationService.requestPermission();
if (permission === 'granted') {
  // Разрешение получено
}
```

#### `show(config: NotificationConfig): Promise<void>`
Показ уведомления.

```typescript
await NotificationService.show({
  title: 'Напоминание',
  body: 'Время выполнить привычку: Зарядка',
  icon: '💪',
  tag: 'habit-123'
});
```

---

## 🔧 NotificationConfig

```typescript
interface NotificationConfig {
  title: string;              // Заголовок уведомления
  body?: string;              // Текст уведомления
  icon?: string;              // Иконка (URL или emoji)
  tag?: string;               // Тег для группировки
  data?: any;                 // Произвольные данные
  requireInteraction?: boolean; // Требует ли взаимодействия
  silent?: boolean;           // Беззвучное уведомление
}
```

---

## 🎨 Особенности

1. **Статический класс** - не требует инстанцирования
2. **Автоматическое закрытие** - через 10 секунд
3. **Фокус на окне** - при клике на уведомление
4. **Graceful degradation** - корректная обработка отсутствия API

---

## 🔗 Где используется

- `NotificationScheduler` (для показа запланированных уведомлений)
- `HabitsNotificationManager` (для запроса разрешений)

---

## 📝 История

- **22 ноября 2025** - Создан как часть унификации notification системы
