# 🏗️ /shared/constructors

> **Назначение:** Композитные конструкторы для сложных компонентов  
> **Паттерн:** Radix UI / shadcn style API  
> **Дата создания:** 21 ноября 2025  
> **Дата обновления:** 22 ноября 2025

---

## 📖 Что такое Constructor Pattern?

**Constructor Pattern** — это подход к созданию компонентов, при котором сложный компонент разбивается на несколько под-компонентов, которые можно комбинировать для получения нужного результата.

### Преимущества:
- ✅ **Гибкость** — можно собрать нужную конфигурацию из частей
- ✅ **Переиспользование** — каждая часть используется независимо
- ✅ **Читаемость** — декларативный API, понятно что внутри
- ✅ **Масштабируемость** — легко добавлять новые части

---

## 📦 Текущие конструкторы:

### 1. Modal (модальные окна)
**Путь:** `/shared/constructors/modal/`  
**Статус:** ✅ Готов к использованию

**Использование:**
```tsx
<Modal.Root level="dialog" onClose={handleClose}>
  <Modal.Backdrop onClick={handleClose} />
  <Modal.Content size="md">
    <Modal.Header title="Заголовок" onClose={handleClose} />
    <div className="p-6">Контент</div>
    <Modal.Footer>
      <Button>Кнопки</Button>
    </Modal.Footer>
  </Modal.Content>
</Modal.Root>
```

**Компоненты:**
- `Modal.Root` — корневой контейнер с порталом
- `Modal.Backdrop` — полупрозрачный фон
- `Modal.Content` — белое окно с размерами
- `Modal.Header` — заголовок с кнопкой закрытия
- `Modal.Footer` — футер для кнопок
- `Modal.CloseButton` — кнопка закрытия

---

### 2. Dropdown (выпадающие меню)
**Путь:** `/shared/constructors/dropdown/`  
**Статус:** ✅ Готов к использованию (22 ноября 2025)  
**Экономия:** ~280 строк кода в 5 компонентах

**Использование:**
```tsx
<Dropdown.Root isOpen={isOpen} onToggle={toggle}>
  <Dropdown.Trigger>
    <span>{value || 'Выберите'}</span>
    <ChevronDown className="w-4 h-4" />
  </Dropdown.Trigger>
  
  <Dropdown.Content direction="down" width="full">
    <Dropdown.Item 
      selected={value === 'option1'}
      onClick={() => setValue('option1')}
    >
      Опция 1
    </Dropdown.Item>
    
    <Dropdown.Separator />
    
    <Dropdown.Item icon={Plus} onClick={handleAdd}>
      Добавить
    </Dropdown.Item>
  </Dropdown.Content>
</Dropdown.Root>
```

**Компоненты:**
- `Dropdown.Root` — корневой контейнер с useDropdown хуком
- `Dropdown.Trigger` — кнопка открытия (полностью кастомизируемая)
- `Dropdown.Content` — выпадающее меню с позиционированием
- `Dropdown.Item` — элемент списка с hover/selected состояниями
- `Dropdown.Separator` — разделитель между группами

**Используется в:**
- CategoryPicker
- IconPicker
- UnitPicker
- TargetTypePicker
- HabitTypePicker

---

## 🎯 Планируемые конструкторы:

### 3. Button (кнопки)
**Статус:** 🚧 Планируется  
**Описание:** Гибридная система кнопок с композитным подходом

### 4. StatCard (карточки статистики)
**Статус:** 💡 Идея  
**Описание:** Композитные карточки для отображения статистики

### 5. FormField (поля форм)
**Статус:** 💡 Идея  
**Описание:** Конструктор для создания полей форм с валидацией

---

## 📁 Структура конструктора:

Каждый конструктор должен иметь такую структуру:

```
/shared/constructors/
  /<constructor-name>/
    <ConstructorName>.tsx   # Основной файл с компонентами
    index.ts                # Barrel export
    README.md               # Документация и примеры
    <ConstructorName>.example.tsx  # (опционально) Примеры использования
```

---

## 🎨 API Design Guidelines:

1. **Namespace Export** — экспортируем как объект: `export const Modal = { ... }`
2. **Composition** — разбиваем на независимые части
3. **Декларативность** — API должен быть понятным и читаемым
4. **Гибкость** — каждая часть работает независимо
5. **TypeScript** — все типы должны быть экспортированы
6. **Документация** — каждый конструктор имеет README.md с примерами

---

## 📚 Документация:

- [Modal система](/docs/ui-systems/MODAL_SYSTEM.md)
- [Dropdown конструктор](/shared/constructors/dropdown/README.md)
- [Паттерны компонентов](/docs/getting-started/COMPONENT_PATTERNS.md)

---

**Дата последнего обновления:** 22 ноября 2025 (добавлен Dropdown)