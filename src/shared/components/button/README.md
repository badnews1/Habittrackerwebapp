# 🔘 /shared/components/button

> **Назначение:** Универсальный компонент кнопки  
> **Статус:** ✅ Мигрировано (21 ноября 2025)  
> **Последнее обновление:** 21 ноября 2025

---

## 📦 Содержимое

### `Button.tsx`
Универсальный компонент кнопки с поддержкой различных вариантов и размеров.

**Пропсы:**
- `variant?: 'primary' | 'secondary' | 'danger' | 'ghost'` - вариант стиля (по умолчанию: `'primary'`)
- `size?: 'sm' | 'md' | 'lg'` - размер кнопки (по умолчанию: `'md'`)
- `fullWidth?: boolean` - растянуть на всю ширину (по умолчанию: `false`)
- `className?: string` - дополнительные CSS классы
- `children: React.ReactNode` - содержимое кнопки
- `...props` - все стандартные пропсы HTML `<button>`

**Варианты (variant):**
- `primary` - основная кнопка (чёрный фон, белый текст)
- `secondary` - вторичная кнопка (серая рамка, серый текст)
- `danger` - опасное действие (красный фон, белый текст)
- `ghost` - прозрачная кнопка (только текст)

**Размеры (size):**
- `sm` - маленькая (`px-3 py-1.5 text-sm`)
- `md` - средняя (`px-6 py-3 text-[14px]`)
- `lg` - большая (`px-8 py-4 text-base`)

---

## 💡 Использование

```typescript
import { Button } from '@/shared/components/button';

// Основная кнопка
<Button variant="primary" onClick={handleSave}>
  Сохранить
</Button>

// Вторичная кнопка
<Button variant="secondary" onClick={handleCancel}>
  Отмена
</Button>

// Опасное действие
<Button variant="danger" onClick={handleDelete}>
  Удалить
</Button>

// Маленькая кнопка
<Button variant="ghost" size="sm">
  Закрыть
</Button>

// Кнопка на всю ширину
<Button variant="primary" fullWidth>
  Продолжить
</Button>

// Отключённая кнопка
<Button variant="primary" disabled>
  Недоступно
</Button>
```

---

## 📥 История миграции

- **Откуда:** `/components/common/Button.tsx`
- **Когда:** 21 ноября 2025
- **Обновлено импортов:** 11 файлов
  1. `/components/calendar/MonthYearPicker.tsx`
  2. `/components/dev/DebugPanel.tsx`
  3. `/components/habits/HabitStatisticsModal.tsx`
  4. `/components/habits/add/FrequencyModal.tsx`
  5. `/components/habits/manage/CategoryPicker.tsx`
  6. `/components/habits/manage/ManageHabitsFooter.tsx`
  7. `/components/modals/ConfirmDialog.tsx`
  8. `/components/modals/ModalFooter.tsx`
  9. `/components/modals/NumericInputModal.tsx`
  10. `/components/notifications/NotificationPermissionBanner.tsx`
  11. `/test-path-aliases.tsx`

---

## ✅ Преимущества

- ✅ Устраняет дублирование стилей кнопок (~10+ мест использования)
- ✅ Единая точка управления внешним видом кнопок
- ✅ Типобезопасность через TypeScript
- ✅ Поддержка всех стандартных HTML атрибутов
- ✅ Автоматическая обработка disabled состояния
- ✅ Гибкая кастомизация через `className`

---

## 🎨 Стили

**Базовые:**
- `rounded-lg` - скруглённые углы
- `transition-colors` - плавный переход цветов
- `inline-flex items-center justify-center` - центрирование контента

**Состояния:**
- `hover:*` - изменение цвета при наведении
- `disabled:opacity-50 disabled:cursor-not-allowed` - отключённое состояние

---

## 🔄 Связанные компоненты

- `/components/ui/button.tsx` - shadcn/ui Button (защищённый, НЕ связан с этим компонентом)
- `/components/modals/ModalFooter.tsx` - использует этот Button
- `/components/modals/ConfirmDialog.tsx` - использует этот Button

---

## ⚠️ Важно

Этот компонент **НЕ связан** с `/components/ui/button.tsx` из shadcn/ui. Это два разных компонента для разных целей:
- **Этот Button** - кастомный компонент для бизнес-логики приложения
- **shadcn/ui Button** - защищённый компонент UI библиотеки (НЕ трогаем!)
