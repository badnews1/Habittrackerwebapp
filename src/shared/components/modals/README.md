# 🪟 /shared/components/modals

> **Назначение:** Универсальные модальные окна для переиспользования  
> **Статус:** ✅ Частично мигрировано (21 ноября 2025)

---

## 📦 Текущие компоненты

### ✅ ConfirmDialog.tsx
Универсальный диалог подтверждения критических действий

**Props:**
- `title: string` - Заголовок диалога
- `message: string` - Текст сообщения
- `confirmText?: string` - Текст кнопки подтверждения (по умолчанию "Подтвердить")
- `cancelText?: string` - Текст кнопки отмены (по умолчанию "Отмена")
- `variant?: 'danger' | 'warning'` - Вариант стиля кнопки подтверждения
- `onConfirm: () => void` - Коллбек подтверждения
- `onCancel: () => void` - Коллбек отмены

**Использование:**
```tsx
import { ConfirmDialog } from '@/shared/components/modals';

<ConfirmDialog
  title="Удалить элемент?"
  message="Это действие нельзя отменить"
  confirmText="Удалить"
  cancelText="Отмена"
  variant="danger"
  onConfirm={handleDelete}
  onCancel={handleCancel}
/>
```

---

## 🚧 Планируется мигрировать

- Другие универсальные модальные окна (пока не определены)

---

## 📍 Специфичные модальные окна

**НЕ мигрируются в shared, остаются в модулях:**
- `/components/modals/AppModals.tsx` - Менеджер модалок модуля привычек
- `/components/modals/DeleteDialog.tsx` - Habits-specific обёртка над ConfirmDialog
- `/components/modals/ModalFooter.tsx` - Футер для AddHabitModal
- `/components/modals/NumericInputModal.tsx` - Ввод значений измеримых привычек

---

## 🔗 Связь с системой модальных окон

Модальные окна используют конструктор из `/shared/constructors/modal/` (Modal.Root, Modal.Backdrop, Modal.Content и т.д.)

**Дата создания:** 21 ноября 2025  
**Последнее обновление:** 21 ноября 2025