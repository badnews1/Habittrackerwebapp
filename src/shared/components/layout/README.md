# 🏗️ /shared/components/layout - Layout компоненты

> **Назначение:** Структурные компоненты для построения layout приложения  
> **Статус:** ✅ Мигрировано (21 ноября 2025)  
> **Последнее обновление:** 21 ноября 2025

---

## 📦 Содержимое

### `Sidebar.tsx`
Боковая навигационная панель приложения.

**Назначение:**  
Полноэкранный sidebar с навигацией по основным секциям: Dashboard, Трекер привычек, Аналитика, Календарь, Настройки.

**Props:**
```typescript
interface SidebarProps {
  currentSection: string;       // Текущая активная секция
  onSectionChange: (section: string) => void;  // Callback при смене секции
  isOpen: boolean;              // Открыт ли sidebar
  onClose: () => void;          // Callback закрытия sidebar
}
```

**Особенности:**
- ✅ Фиксированная позиция слева (w-64, h-screen)
- ✅ Плавная анимация slide-in/slide-out (transition-transform duration-300)
- ✅ Секция Dashboard отдельно от других
- ✅ Поддержка disabled состояния для секций
- ✅ Активная секция подсвечивается (bg-gray-900 text-white)
- ✅ Настройки в нижней части (pinned to bottom)
- ✅ Респонсивная кнопка закрытия
- ✅ Shadow overlay для выделения

**Дизайн:**
- Logo/Brand area (px-6 py-8)
- Navigation menu с иконками
- Settings button внизу (border-top)
- Минималистичный стиль в духе Джонни Айва

---

### `VersionIndicator.tsx`
Индикатор версии приложения.

**Назначение:**  
Показывает текущую версию приложения в нижнем левом углу экрана.

**Props:**  
Нет (stateless component)

**Особенности:**
- ✅ Фиксированная позиция (bottom-2 left-2)
- ✅ Моноширинный шрифт (font-mono)
- ✅ Светло-серый цвет для ненавязчивости (text-gray-300)
- ✅ Очень маленький размер (text-[10px])
- ✅ Tooltip при наведении ("Версия приложения")
- ✅ Высокий z-index (z-50) для видимости поверх контента

**Текущая версия:** v1.2.4

---

## 💡 Использование

### Sidebar

```typescript
import { Sidebar } from '@/shared/components/layout';

function App() {
  const [currentSection, setCurrentSection] = useState('habits');
  const [isSidebarOpen, setIsSidebarOpen] = useState(false);

  return (
    <div className="flex min-h-screen">
      <Sidebar
        currentSection={currentSection}
        onSectionChange={setCurrentSection}
        isOpen={isSidebarOpen}
        onClose={() => setIsSidebarOpen(false)}
      />
      
      <main className="flex-1">
        {/* Основной контент */}
      </main>
    </div>
  );
}
```

### С Zustand store

```typescript
import { Sidebar } from '@/shared/components/layout';
import { useHabitsStore } from '@/core/store';

function App() {
  const currentSection = useHabitsStore(state => state.currentSection);
  const isSidebarOpen = useHabitsStore(state => state.isSidebarOpen);
  const setCurrentSection = useHabitsStore(state => state.setCurrentSection);
  const toggleSidebar = useHabitsStore(state => state.toggleSidebar);

  return (
    <Sidebar 
      currentSection={currentSection}
      onSectionChange={setCurrentSection}
      isOpen={isSidebarOpen}
      onClose={() => toggleSidebar(false)}
    />
  );
}
```

### VersionIndicator

```typescript
import { VersionIndicator } from '@/shared/components/layout';

function App() {
  return (
    <div className="app">
      {/* Ваш контент */}
      
      {/* Индикатор версии (обычно в конце) */}
      <VersionIndicator />
    </div>
  );
}
```

### Импорт обоих компонентов

```typescript
import { Sidebar, VersionIndicator } from '@/shared/components/layout';
```

---

## 🎨 Дизайн

### Sidebar секции

**Доступные секции:**
1. **Dashboard** (отдельно, disabled) - LayoutDashboard icon
2. **Трекер привычек** (активна) - GoalIcon
3. **Аналитика** (disabled) - BarChart3 icon
4. **Календарь** (disabled) - Calendar icon
5. **Настройки** (внизу, disabled) - Settings icon

**Состояния кнопок:**
- **Активная:** `bg-gray-900 text-white`
- **Обычная:** `text-gray-600 hover:bg-gray-50 hover:text-gray-900`
- **Disabled:** `text-gray-300 cursor-not-allowed`

### Анимация Sidebar

```css
/* Открыт */
translate-x-0

/* Закрыт */
-translate-x-full

/* Transition */
transition-transform duration-300
```

### VersionIndicator стиль

```css
position: fixed;
bottom: 0.5rem;
left: 0.5rem;
font-size: 10px;
color: rgb(209, 213, 219); /* gray-300 */
font-family: monospace;
z-index: 50;
```

---

## 📥 История миграции

### Sidebar
- **Откуда:** `/components/layout/Sidebar.tsx`
- **Куда:** `/shared/components/layout/Sidebar.tsx`
- **Когда:** 21 ноября 2025
- **Обновлено импортов:** 1 файл (`/App.tsx`)
- **Изменения:**
  - ✅ Обновлён импорт иконок с `../icons` на `@/shared/icons`
  - ✅ Добавлены JSDoc комментарии
  - ✅ Улучшена документация Props

### VersionIndicator
- **Откуда:** `/components/layout/VersionIndicator.tsx`
- **Куда:** `/shared/components/layout/VersionIndicator.tsx`
- **Когда:** 21 ноября 2025
- **Обновлено импортов:** 1 файл (`/App.tsx`)
- **Изменения:**
  - ✅ Добавлены JSDoc комментарии
  - ✅ Сохранён React.FC тип

---

## 🔧 Техническая информация

### Зависимости

**Sidebar:**
- `@/shared/icons` - иконки для навигации
- React (для JSX)
- Tailwind CSS (для стилей)

**VersionIndicator:**
- React (только базовый импорт)
- Tailwind CSS (для стилей)

### TypeScript типы

```typescript
// Sidebar Props
interface SidebarProps {
  currentSection: string;
  onSectionChange: (section: string) => void;
  isOpen: boolean;
  onClose: () => void;
}

// VersionIndicator Props
// Нет props (React.FC без generic параметра)
```

---

## 📊 Статистика

| Компонент | Строк кода | Импортов | Используется в |
|-----------|------------|----------|----------------|
| Sidebar | ~125 | 1 файл | `/App.tsx` |
| VersionIndicator | ~30 | 1 файл | `/App.tsx` |
| **Итого** | **~155** | **2 импорта** | **1 файл** |

---

## ⚡ Производительность

### Sidebar
- ✅ Чистый React без хуков (кроме props)
- ✅ Никаких тяжёлых вычислений
- ✅ CSS transitions вместо JS анимаций
- ✅ Секции рендерятся через .map() (эффективно)

### VersionIndicator
- ✅ Stateless компонент
- ✅ Минимальный размер (~30 строк)
- ✅ Нет рендеров после монтирования
- ✅ Нулевые зависимости

---

## 🎯 Когда использовать

### Sidebar
Использу когда нужно:
- ✅ Навигация между разными секциями приложения
- ✅ Боковое меню с несколькими разделами
- ✅ Фиксированная панель навигации
- ✅ Mobile-friendly меню (slide-in/out)

НЕ используй для:
- ❌ Временных панелей (используй Sheet/Drawer)
- ❌ Контекстных меню (используй Dropdown)
- ❌ Модальных окон (используй Modal)

### VersionIndicator
Используй когда нужно:
- ✅ Показать версию приложения
- ✅ Минималистичный индикатор в углу
- ✅ Информация для разработчиков/тестирования

НЕ используй для:
- ❌ Важных уведомлений (используй Toast/Alert)
- ❌ Интерактивных элементов (это только текст)

---

## 🔄 Связанные компоненты

### См. также:
- `/shared/icons/` - иконки для Sidebar
- `/shared/components/button/` - если нужны кастомные кнопки
- `/core/store/` - Zustand store для управления состоянием
- `/components/dev/` - dev инструменты (DebugPanel)

---

## 🚀 Будущие улучшения

### Sidebar
- [ ] Добавить анимацию иконок при hover
- [ ] Сделать конфигурируемым (передавать секции как props)
- [ ] Добавить поддержку вложенных секций
- [ ] Добавить keyboard navigation (Arrow keys)
- [ ] Backdrop overlay при открытии на мобильных

### VersionIndicator
- [ ] Сделать кликабельным с changelog modal
- [ ] Добавить цветовую индикацию (dev/staging/prod)
- [ ] Показывать Git commit hash в dev режиме
- [ ] Добавить автоматическое обновление из package.json

---

## ⚠️ Важные примечания

### Sidebar
1. **z-index:** Используется `z-50` для отображения поверх контента
2. **Backdrop:** Нет backdrop overlay (можно добавить при необходимости)
3. **Disabled секции:** Визуально выделены серым + `cursor-not-allowed`
4. **Ширина:** Фиксированная `w-64` (256px)
5. **Mobile:** На мобильных открывается поверх контента (fixed + transform)

### VersionIndicator
1. **z-index:** Использует `z-50` для видимости
2. **Позиция:** Фиксированная, не скроллится с контентом
3. **Обновление версии:** Вручную в коде (TODO: автоматизировать)
4. **Конфликт с DebugPanel:** Оба в левом нижнем углу, но DebugPanel выше

---

## 📝 Примеры кастомизации

### Изменить ширину Sidebar

```typescript
// В Sidebar.tsx
<aside className="... w-80"> {/* Было: w-64 */}
```

### Добавить backdrop для Sidebar

```typescript
function App() {
  return (
    <>
      {/* Backdrop */}
      {isSidebarOpen && (
        <div 
          className="fixed inset-0 bg-black/50 z-40"
          onClick={() => toggleSidebar(false)}
        />
      )}
      
      <Sidebar {...props} />
    </>
  );
}
```

### Изменить версию приложения

```typescript
// В VersionIndicator.tsx
const APP_VERSION = '1.3.0'; // Обновить здесь
```

### Кастомная позиция VersionIndicator

```typescript
// Вместо bottom-left, сделать top-right
<div className="fixed top-2 right-2 text-[10px] text-gray-300 font-mono z-50">
  v{APP_VERSION}
</div>
```