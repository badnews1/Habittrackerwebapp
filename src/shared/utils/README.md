# 🛠️ /shared/utils - Общие утилиты

> **Дата создания:** 21 ноября 2025  
> **Последнее обновление:** 22 ноября 2025  
> **Статус:** ✅ Завершено  
> **Назначение:** Универсальные вспомогательные функции для всего приложения

---

## 📁 Структура

```
/shared/utils/
  ├── /logger/       # ✅ Централизованная система логирования
  ├── /date/         # ✅ Работа с датами
  └── /text/         # ✅ Склонение слов и форматирование
```

---

## ✅ Мигрированные утилиты (21 ноября 2025)

### 1️⃣ `/logger/` - Централизованное логирование
**Файлы:** `logger.ts`, `index.ts`, `README.md`

**Назначение:**  
Единая система логирования с поддержкой уровней, модулей, фильтрации и управления через консоль.

**Основной API:**
```typescript
import { logger, habitLogger, storageLogger } from '@/shared/utils/logger';

logger.debug('Debug message', data);
logger.info('Info message');
logger.warn('Warning');
logger.error('Error', error);
logger.success('Success!');

habitLogger.info('Habit added', habit);
storageLogger.debug('Reading from storage');
```

**Особенности:**
- ✅ 5 уровней логирования (debug, info, warn, error, success)
- ✅ 10 предсозданных модульных логгеров
- ✅ Фильтрация по модулям (whitelist/blacklist)
- ✅ Управление через `window.__logger` в dev режиме
- ✅ Автоотключение в production (кроме ?debug=true)

**Миграция:**
- Откуда: `/utils/logger.ts`
- Обновлено импортов: 7 файлов

---

### 2️⃣ `/date/` - Работа с датами
**Файлы:** `dateUtils.ts`, `index.ts`

**Назначение:**  
Функции для форматирования дат, получения дней месяца, работы с русскими названиями.

**API:**
```typescript
import { 
  formatDate, 
  getDaysInMonth, 
  getDayName, 
  formatDateReadable,
  DAY_NAMES_SHORT,
  MONTH_NAMES_GENITIVE 
} from '@/shared/utils/date';

formatDate(new Date()); // '2025-11-21'
getDaysInMonth(10, 2025); // [{ date, day }, ...]
getDayName(new Date()); // 'Чт'
formatDateReadable('2025-11-21'); // 'Чт, 21 ноября'
```

**Константы:**
- `DAY_NAMES_SHORT` - ['Вс', 'Пн', 'Вт', ...]
- `MONTH_NAMES_GENITIVE` - ['января', 'февраля', ...]

**Миграция:**
- Откуда: `/utils/dateUtils.ts`
- Обновлено импортов: 7 файлов (включая App.tsx)

---

### 3️⃣ `/text/` - Склонение и форматирование
**Файлы:** `textUtils.ts`, `index.ts`

**Назначение:**  
Склонение русских слов, единиц измерения, форматирование чисел.

**API:**
```typescript
import { 
  decline,
  declineDays,
  declineTimes,
  declineHabits,
  declineUnit,
  getShortUnit,
  formatNumber 
} from '@/shared/utils/text';

// Базовое склонение
decline(5, 'день', 'дня', 'дней'); // 'дней'

// Готовые функции
declineDays(1); // 'день'
declineDays(2); // 'дня'
declineDays(5); // 'дней'
declineHabits(21); // 'привычка'

// Единицы измерения
declineUnit(1, 'разы'); // 'раз'
declineUnit(2, 'литры'); // 'литра'
declineUnit(1.5, 'килограммы'); // 'килограмма'

// Сокращения
getShortUnit('километры'); // 'км'
getShortUnit('минуты'); // 'мин'

// Форматирование больших чисел
formatNumber(999); // '999'
formatNumber(10000); // '10k'
formatNumber(1500000); // '1.5M'
```

**Поддерживаемые функции:**
- Склонение общих слов (дни, разы, привычки, напоминания)
- Склонение для частоты (раз в неделю, раз в месяц, раз в N дней)
- Склонение 21 единицы измерения
- Форматирование с числом
- Сокращения для календарной сетки

**Миграция:**
- Откуда: `/utils/textUtils.ts`
- Обновлено импортов: 8 файлов

---

## 🚧 Ожидают миграции

Эти утилиты останутся в `/utils/` до создания `/features/habits/`:

### Фича-специфичные утилиты (для habits):
- `habitUtils.ts` - логика работы с привычками
- `frequencyValidation.ts` - валидация частоты
- `strengthCalculator.ts` - расчёт силы привычки (EMA)
- `strengthHistory.ts` - история силы
- `initializeCategories.ts` - инициализация категорий

---

## 🎯 Правило размещения

**Утилита попадает в `/shared/utils`, если:**
- ✅ Используется в 2+ фичах (или потенциально может быть)
- ✅ Не содержит бизнес-логики конкретной фичи
- ✅ Имеет чистый, переиспользуемый интерфейс
- ✅ Универсальна и не зависит от доменных типов

**Утилита остаётся в `/features/*/utils`, если:**
- ❌ Используется только одной фичей
- ❌ Содержит специфичную бизнес-логику
- ❌ Тесно связана с доменными типами фичи
- ❌ Не имеет смысла вне контекста фичи

---

## 📊 Статистика миграции

| Утилита | Откуда | Импортов | Размер |
|---------|--------|----------|--------|
| logger | `/utils/` | 7 | ~620 строк |
| date | `/utils/` | 7 | ~110 строк |
| text | `/utils/` | 8 | ~400 строк |
| **Итого** | | **22** | **~1130 строк** |

---

## 📚 Примеры использования

### Комбинированное использование

```typescript
import { formatDate, getDaysInMonth } from '@/shared/utils/date';
import { declineDays, formatNumber } from '@/shared/utils/text';
import { habitLogger } from '@/shared/utils/logger';

// Получаем дни месяца
const days = getDaysInMonth(10, 2025);
habitLogger.info(`Месяц содержит ${days.length} ${declineDays(days.length)}`);

// Форматируем дату
const today = formatDate(new Date());
habitLogger.debug('Сегодняшняя дата', { today });

// Обрабатываем большие числа
const steps = 15000;
habitLogger.success(`Пройдено ${formatNumber(steps)} шагов`);
```

### В компонентах

```typescript
// В CalendarHeader
import { getDaysInMonth, formatDate } from '@/shared/utils/date';

const monthDays = getDaysInMonth(selectedMonth, selectedYear);

// В HabitCheckboxCell
import { declineUnit, getShortUnit, formatNumber } from '@/shared/utils/text';

const displayValue = formatNumber(value);
const unit = getShortUnit(habit.unit);
const declined = declineUnit(value, habit.unit);

// В Zustand store
import { habitLogger, storageLogger } from '@/shared/utils/logger';

habitLogger.info('Добавлена привычка', habit);
storageLogger.debug('Сохранение в localStorage');
```

---

## 🔄 Связанные файлы

### См. также:
- `/docs/FEATURE_BASED_REFACTORING.md` - план миграции
- `/docs/ARCHITECTURE_AUDIT.md` - архитектурный аудит
- `/docs/MIGRATION_LOG.md` - журнал миграции
- `/shared/components/` - общие компоненты
- `/shared/hooks/` - общие хуки
- `/shared/types/` - общие типы (будущее)

---

## ⚠️ Важные примечания

### Алиасы для импортов
```typescript
// ✅ Правильно - используем алиасы
import { logger } from '@/shared/utils/logger';
import { formatDate } from '@/shared/utils/date';
import { decline } from '@/shared/utils/text';

// ❌ Неправильно - старые относительные пути
import { logger } from '../../utils/logger';
import { formatDate } from '../../../utils/dateUtils';
```

### Barrel exports
Все утилиты экспортируются через `index.ts` для удобства:
```typescript
// Можно импортировать напрямую из папки
import { logger, habitLogger } from '@/shared/utils/logger';
// Это то же самое что:
import { logger, habitLogger } from '@/shared/utils/logger/logger';
```

### TypeScript типы
Все утилиты полностью типизированы:
```typescript
// decline принимает number | undefined
decline(undefined, 'день', 'дня', 'дней'); // 'дней'

// formatDate принимает Date
formatDate(new Date()); // '2025-11-21'
```