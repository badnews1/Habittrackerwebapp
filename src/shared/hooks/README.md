# 🎣 /shared/hooks - Общие React хуки

> **Дата создания:** 21 ноября 2025  
> **Последнее обновление:** 22 ноября 2025  
> **Статус:** ✅ Завершено  
> **Назначение:** Переиспользуемые React хуки для нескольких фич

---

## 📁 Структура

```
/shared/hooks/
  ├── /use-click-outside/    # ✅ Обнаружение кликов вне элемента
  └── /use-dropdown/         # ✅ Управление dropdown состоянием
```

---

## ✅ Мигрированные хуки (21 ноября 2025)

### 1️⃣ `/use-click-outside/` - Клик вне элемента
**Файлы:** `useClickOutside.ts`, `index.ts`, `README.md`

**Назначение:**  
Универсальный хук для обнаружения кликов вне указанного элемента. Используется для закрытия dropdown, модальных окон и других UI элементов.

**Параметры:**
- `ref: RefObject<HTMLElement>` - элемент для отслеживания
- `handler: () => void` - функция при клике снаружи
- `enabled?: boolean` - включить/отключить (по умолчанию: `true`)

**Использование:**
```typescript
import { useClickOutside } from '@/shared/hooks/use-click-outside';

const dropdownRef = useRef<HTMLDivElement>(null);
useClickOutside(dropdownRef, () => setIsOpen(false), isOpen);
```

**Миграция:**
- Откуда: `/hooks/useClickOutside.ts`
- Обновлено импортов: 2 файла

---

### 2️⃣ `/use-dropdown/` - Управление dropdown
**Файлы:** `useDropdown.ts`, `index.ts`, `README.md`

**Назначение:**  
Универсальный хук для управления dropdown компонентами с клавиатурной навигацией.

**Использование:**
```typescript
import { useDropdown } from '@/shared/hooks/use-dropdown';
```

📚 [Полная документация →](./use-dropdown/README.md)

**Импорты обновлены:** 6 файлов (CategoryPicker, IconPicker, etc.)  
**Устранено дублирования:** ~750 строк кода!

---

### 3️⃣ `/use-habits-filter/` - Фильтрация привычек
**Файлы:** `useHabitsFilter.ts`, `index.ts`, `README.md`

**Назначение:**  
Универсальный хук для фильтрации привычек по категориям и типам.

**Использование:**
```typescript
import { useHabitsFilter } from '@/shared/hooks/use-habits-filter';

const { state, actions, result } = useHabitsFilter(habits, {
  initialState: { isCategoryExpanded: false },
});

// Отфильтрованные привычки
const { filteredHabits, hasActiveFilters } = result;
```

📚 [Полная документация →](./use-habits-filter/README.md)

**Импорты обновлены:** 1 файл (`ManageHabitsModal.tsx`)  
**Связан с:** `HabitsFilterDropdown` компонент

---

## 🚧 Ожидают миграции

Эти хуки останутся в `/hooks/` до создания `/features/habits/`:

### Фича-специфичные хуки (для habits):
- `useCategoriesManager.ts` - управление категориями
- `useFrequencyModal.ts` - модальное окно частоты
- `useFrequencyState.ts` - состояние частоты
- `useRulerMode.ts` - режим линейки
- `useRulerState.ts` - состояние линейки
- `useStrengthUpdater.ts` - обновление силы привычки

---

## 🎯 Правило размещения

**Хук попадает в `/shared/hooks`, если:**
- ✅ Используется в 2+ фичах (или потенциально может быть)
- ✅ Не содержит бизнес-логики конкретной фичи
- ✅ Имеет чистый, переиспользуемый интерфейс
- ✅ Универсален и не зависит от доменных типов

**Хук остаётся в `/features/*/hooks`, если:**
- ❌ Используется только одной фичей
- ❌ Содержит специфичную бизнес-логику
- ❌ Тесно связан с доменными типами фичи
- ❌ Не имеет смысла вне контекста фичи

---

## 📊 Статистика миграции

| Хук | Откуда | Импортов | Экономия кода |
|-----|--------|----------|---------------|
| useClickOutside | `/hooks/` | 2 | - |
| useDropdown | `/hooks/` | 6 | ~750 строк |
| useHabitsFilter | `/hooks/` | 1 | - |
| **Итого** | | **9** | **~750 строк** |

---

## 📚 См. также

- `/docs/FEATURE_BASED_REFACTORING.md` - план миграции
- `/docs/ARCHITECTURE_AUDIT.md` - аудит архитектуры
- `/docs/MIGRATION_LOG.md` - журнал миграции
- `/shared/components/` - общие компоненты
- `/shared/utils/` - общие утилиты