# 🎯 Фильтрация логов по модулям

> **Новая возможность!** Управляйте отображением логов по модулям для удобной отладки.  
> **Дата обновления:** 22 ноября 2025

---

## 📖 Что это?

Фильтрация по модулям позволяет показывать или скрывать логи от конкретных частей приложения.

**Зачем?** При отладке силы привычки не нужны логи от UI, Storage и других модулей. Фильтр очищает консоль, оставляя только важное.

---

## 🎯 Показать только определённые модули (белый список)

### Синтаксис

```javascript
__logger.showOnly('МОДУЛЬ1', 'МОДУЛЬ2', ...)
```

### Примеры

```javascript
// Показать только логи силы привычки
__logger.showOnly('STRENGTH')
// 🎯 Показываем только модули: STRENGTH

// Показать расчёты силы и частоты
__logger.showOnly('STRENGTH', 'FREQUENCY')
// 🎯 Показываем только модули: STRENGTH, FREQUENCY

// Показать всё, что связано с данными
__logger.showOnly('HABITS', 'STORAGE', 'CATEGORIES')
// 🎯 Показываем только модули: HABITS, STORAGE, CATEGORIES
```

### Результат

Теперь в консоли видны **только** логи указанных модулей:

```
ℹ️ [INFO] [STRENGTH] Recalculating habit strength
🔍 [DEBUG] [STRENGTH] Current EMA: 0.85
🔍 [DEBUG] [FREQUENCY] Checking daily frequency
✅ [SUCCESS] [STRENGTH] Strength updated: 87%
```

Логи других модулей (HABITS, UI, STORAGE...) **скрыты**.

---

## 🚫 Скрыть определённые модули (чёрный список)

### Синтаксис

```javascript
__logger.hide('МОДУЛЬ1', 'МОДУЛЬ2', ...)
```

### Примеры

```javascript
// Скрыть шумные модули UI и STORAGE
__logger.hide('UI', 'STORAGE')
// 🚫 Скрываем модули: UI, STORAGE

// Скрыть только логи инициализации
__logger.hide('INIT')
// 🚫 Скрываем модули: INIT
```

### Результат

Все модули **кроме** указанных:

```
ℹ️ [INFO] [HABITS] Habit added successfully
🔍 [DEBUG] [STRENGTH] Calculating strength
✅ [SUCCESS] [CATEGORIES] Category created
```

Логи UI и STORAGE **скрыты**, остальные видны.

---

## ✅ Показать все модули (сброс фильтра)

### Синтаксис

```javascript
__logger.showAll()
// или
__logger.clearFilter()
```

### Пример

```javascript
// Отменить фильтрацию
__logger.showAll()
// ✅ Показываем все модули

// Теперь видны логи от всех модулей
```

---

## 📋 Список доступных модулей

### Просмотр списка

```javascript
__logger.modules
```

**Результат:**

```javascript
['HABITS', 'STRENGTH', 'FREQUENCY', 'CATEGORIES', 'STORAGE', 
 'STATS', 'UI', 'REMINDERS', 'VALIDATION', 'INIT']
```

### Описание модулей

| Модуль | Описание |
|--------|----------|
| `HABITS` | Добавление, удаление, редактирование привычек |
| `STRENGTH` | Расчёт силы привычки (EMA) |
| `FREQUENCY` | Настройки частоты выполнения |
| `CATEGORIES` | Управление категориями |
| `STORAGE` | Операции с localStorage |
| `STATS` | Статистика и аналитика |
| `UI` | События интерфейса (клики, открытие модальных окон) |
| `REMINDERS` | Система напоминаний |
| `VALIDATION` | Проверка данных форм |
| `INIT` | Инициализация приложения при загрузке |

---

## 🔍 Проверить текущий фильтр

### Синтаксис

```javascript
__logger.filterStatus()
```

### Примеры

```javascript
// После __logger.showOnly('STRENGTH', 'FREQUENCY')
__logger.filterStatus()
// { mode: 'whitelist', modules: ['STRENGTH', 'FREQUENCY'] }

// После __logger.hide('UI', 'STORAGE')
__logger.filterStatus()
// { mode: 'blacklist', modules: ['UI', 'STORAGE'] }

// После __logger.showAll()
__logger.filterStatus()
// { mode: 'all', modules: [] }
```

### Поля ответа

- **`mode`**: режим фильтрации
  - `'all'` — все модули видны
  - `'whitelist'` — видны только указанные
  - `'blacklist'` — скрыты только указанные
- **`modules`**: массив отфильтрованных модулей

---

## 💡 Практические сценарии

### 1. Отладка силы привычки

**Проблема:** При расчёте силы привычки консоль засорена логами UI, Storage, Categories.

**Решение:**

```javascript
// Показать только логи расчёта силы
__logger.showOnly('STRENGTH')

// Выполнить действие в UI (отметить выполнение привычки)
// Консоль показывает ТОЛЬКО логи STRENGTH

// Вернуть все логи
__logger.clearFilter()
```

**Результат:**

```
ℹ️ [INFO] [STRENGTH] Recalculating: Зарядка
🔍 [DEBUG] [STRENGTH] Previous: 85%, New: 87%
✅ [SUCCESS] [STRENGTH] Updated
```

---

### 2. Убрать шум из консоли

**Проблема:** UI и Storage логируют слишком часто, мешают видеть важные логи.

**Решение:**

```javascript
// Скрыть шумные модули
__logger.hide('UI', 'STORAGE')

// Консоль теперь чище
```

**Результат:** Видны логи HABITS, STRENGTH, CATEGORIES и других модулей, но без UI и STORAGE.

---

### 3. Анализ производительности

**Проблема:** Нужно увидеть только логи, связанные с расчётами и статистикой.

**Решение:**

```javascript
// Показать только модули расчётов
__logger.showOnly('STRENGTH', 'FREQUENCY', 'STATS')

// Выполнить операцию пересчёта всех привычек
```

**Результат:**

```
⏱️ [TIME] [STRENGTH] Recalculation started
🔍 [DEBUG] [FREQUENCY] Checking weekly frequency
ℹ️ [INFO] [STATS] Calculating completion rate
⏱️ [TIME] [STRENGTH] Recalculation: 145.23ms
```

---

### 4. Отладка инициализации

**Проблема:** Приложение медленно загружается, нужно понять почему.

**Решение:**

```javascript
// Показать логи инициализации и Storage
__logger.showOnly('INIT', 'STORAGE')

// Перезагрузить страницу
```

**Результат:**

```
🔍 [DEBUG] [INIT] Loading habits from localStorage
🔍 [DEBUG] [STORAGE] Found 8 habits in storage
ℹ️ [INFO] [INIT] Loaded 8 habits
⏱️ [TIME] [STORAGE] Parse habits: 12.34ms
✅ [SUCCESS] [INIT] Application ready
```

---

## ⚙️ Использование в коде

Фильтрацию можно применять программно:

```typescript
import { showOnlyModules, hideModules, showAllModules } from '../utils/logger';

// Включить фильтр перед операцией
showOnlyModules('STRENGTH', 'FREQUENCY');

// Выполнить операцию
recalculateAllHabitsStrength();

// Вернуть все логи
showAllModules();
```

### Пример с try/finally

```typescript
import { showOnlyModules, showAllModules } from '../utils/logger';

try {
  showOnlyModules('STRENGTH');
  
  // Отладочная операция
  debugStrengthCalculation();
} finally {
  // Гарантированно вернуть все логи
  showAllModules();
}
```

---

## 🚀 Сочетание фильтров

Фильтрация по модулям **комбинируется** с фильтрацией по уровням:

### Пример 1: Только ошибки конкретного модуля

```javascript
// Показать только ошибки и предупреждения от HABITS
__logger.showOnly('HABITS')
__logger.updateConfig({ minLevel: 'warn' })
```

**Результат:**

```
⚠️ [WARN] [HABITS] Duplicate habit name detected
❌ [ERROR] [HABITS] Failed to save habit
```

Логи debug/info от HABITS **скрыты**, логи других модулей **скрыты**.

---

### Пример 2: Все логи кроме debug от UI

```javascript
// Скрыть модуль UI и debug логи
__logger.hide('UI')
__logger.updateConfig({ minLevel: 'info' })
```

**Результат:** Видны info/warn/error от всех модулей, кроме UI.

---

## 🎓 Алиасы и быстрые команды

| Полная команда | Алиас | Описание |
|----------------|-------|----------|
| `__logger.showOnly('STRENGTH')` | `__logger.setFilter('STRENGTH')` | Показать только модуль |
| `__logger.showAll()` | `__logger.clearFilter()` | Сбросить фильтр |

### Примеры

```javascript
// Эквивалентны
__logger.showOnly('STRENGTH')
__logger.setFilter('STRENGTH')

// Эквивалентны
__logger.showAll()
__logger.clearFilter()
```

---

## 📊 Примеры из реальной отладки

### До фильтрации (консоль засорена)

```
🔍 [DEBUG] [UI] Button clicked
🔍 [DEBUG] [STORAGE] Loading habits
ℹ️ [INFO] [HABITS] Found 10 habits
ℹ️ [INFO] [STRENGTH] Recalculating: Зарядка  👈 НУЖНО
🔍 [DEBUG] [STRENGTH] Current: 85% → 87%      👈 НУЖНО
🔍 [DEBUG] [UI] Rendering list
🔍 [DEBUG] [STORAGE] Saving data
✅ [SUCCESS] [STRENGTH] Updated                👈 НУЖНО
🔍 [DEBUG] [UI] Animation finished
```

**Проблема:** Из 9 логов нужны только 3, остальные — шум.

---

### После фильтрации (чистая консоль)

```javascript
__logger.showOnly('STRENGTH')
```

**Результат:**

```
ℹ️ [INFO] [STRENGTH] Recalculating: Зарядка
🔍 [DEBUG] [STRENGTH] Current: 85% → 87%
✅ [SUCCESS] [STRENGTH] Updated
```

**Идеально!** Только нужные логи, ничего лишнего.

---

## 🔧 Отладка самой фильтрации

Если фильтр не работает:

```javascript
// 1. Проверить, что логи вообще включены
__logger.config
// enabled должно быть true

// 2. Проверить статус фильтра
__logger.filterStatus()
// mode: 'whitelist', modules: ['STRENGTH']

// 3. Проверить список модулей
__logger.modules
// Убедиться, что модуль существует

// 4. Сбросить фильтр
__logger.clearFilter()

// 5. Попробовать снова
__logger.showOnly('STRENGTH')
```

---

## ✅ Чеклист: Когда использовать фильтрацию?

- [ ] Отлаживаю конкретный модуль → `showOnly('MODULE')`
- [ ] В консоли слишком много шума → `hide('UI', 'STORAGE')`
- [ ] Анализирую производительность → `showOnly('STATS', 'STRENGTH')`
- [ ] Отлаживаю загрузку приложения → `showOnly('INIT', 'STORAGE')`
- [ ] Хочу вернуть все логи → `clearFilter()`

---

## 🎯 Краткая справка

```javascript
// Показать только модуль STRENGTH
__logger.showOnly('STRENGTH')

// Показать несколько модулей
__logger.showOnly('STRENGTH', 'FREQUENCY', 'STATS')

// Скрыть шумные модули
__logger.hide('UI', 'STORAGE')

// Вернуть все логи
__logger.showAll()

// Проверить статус
__logger.filterStatus()

// Список модулей
__logger.modules
```

---

**Успешной отладки!** 🚀