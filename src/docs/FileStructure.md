# 📁 Структура файлов проекта

> **Дата обновления:** 24 ноября 2025  
> **Архитектура:** Feature-Based с модульной структурой

---

## О проекте

Веб-приложение трекера привычек с минималистичным дизайном в стиле Джонни Айва.
Приложение позволяет добавлять привычки, отмечать их выполнение в календарной сетке и отслеживать прогресс с показом серий и процентов выполнения.
Реализована система "Силы привычки" на основе EMA, функция "заморозки" силы привычки, система двух типов привычек (бинарных и измеримых с 22 единицами измерения) и полноценная система частоты выполнения.

**Архитектура:** Feature-Based с централизованным управлением состоянием через Zustand.

**Всего файлов:** ~170

---

## 🎯 Корень проекта

```
/
├── App.tsx              - Главный компонент приложения
├── README.md            - Быстрый обзор проекта
├── Attributions.md      - Атрибуции и лицензии библиотек
├── tsconfig.json        - Конфигурация TypeScript с path aliases
└── jsconfig.json        - Конфигурация JavaScript/IDE с path aliases
```

### Описание файлов

- **`App.tsx`** - Главный компонент приложения с Zustand store, содержит разметку приложения и подключает все модули
- **`README.md`** - Краткое описание проекта и ссылка на документацию
- **`Attributions.md`** - Список используемых библиотек и их лицензии
- **`tsconfig.json`** - Настройки TypeScript компилятора, path aliases для удобного импорта
- **`jsconfig.json`** - Настройки для IDE и JavaScript, синхронизировано с tsconfig

---

## 🏗️ `/core` - Ядро приложения

**Назначение:** Централизованное глобальное состояние (Zustand Store) и менеджеры модальных окон для всего приложения.

```
/core
├── /store              - Zustand Store с модульной архитектурой
│   ├── index.ts        - Главный файл store (create + persist)
│   ├── types.ts        - TypeScript интерфейсы state и actions
│   ├── initialState.ts - Начальное состояние store
│   └── /slices         - Модули store по функциональности
│       ├── ui.ts       - UI состояние (sidebar, section, date)
│       ├── modals.ts   - Управление модальными окнами
│       ├── addHabitForm.ts - Форма добавления привычки
│       ├── habits.ts   - CRUD операции с привычками
│       ├── tags.ts     - Управление тегами
│       ├── sections.ts - Управление разделами привычек
│       ├── goals.ts    - Управление целями
│       ├── manageHabitsModal.ts - Локальное состояние модалки управления
│       └── internal.ts - Внутренние системные actions
└── /modals             - Менеджеры модальных окон модулей
    ├── HabitTrackerModals.tsx - Менеджер модалок habit-tracker
    └── index.ts        - Barrel export всех менеджеров
```

### `/core/store` - Централизованное состояние

Глобальный Zustand store с модульной архитектурой slices. Каждый slice отвечает за свою область функциональности.

**Основные файлы:**
- **`index.ts`** - Создание store через `create()`, подключение `persist()` middleware, объединение всех slices
- **`types.ts`** - TypeScript интерфейс `HabitsState` со всеми полями и методами
- **`initialState.ts`** - Начальные значения для всех полей state

**Slices:**
- **`ui.ts`** - Состояние UI (открытость sidebar, текущая секция, выбранный месяц/год)
- **`modals.ts`** - Управление всеми модальными окнами приложения
- **`addHabitForm.ts`** - Состояние формы добавления привычки (все поля, шаги, валидация)
- **`habits.ts`** - CRUD операции с привычками, toggle completion, массовые операции, Undo
- **`tags.ts`** - Добавление, удаление, обновление тегов (с проверкой уникальности)
- **`sections.ts`** - Управление разделами привычек
- **`goals.ts`** - Управление месячными целями для привычек
- **`manageHabitsModal.ts`** - Локальное состояние модального окна управления (позволяет редактировать без изменения глобального state до сохранения)
- **`internal.ts`** - Служебные actions (инкремент счетчика, обновление силы привычек, очистка previousState)

### `/core/modals` - Менеджеры модальных окон

Централизованные менеджеры модальных окон для разных модулей приложения. Каждый модуль имеет свой менеджер, который рендерит все модалки модуля в одном месте.

**Файлы:**
- **`HabitTrackerModals.tsx`** - Менеджер модальных окон модуля habit-tracker (MonthYearPicker, ManageHabitsModal, AddHabitModal)
- **`index.ts`** - Barrel export всех менеджеров

---

## 📦 `/modules` - Модули приложения

**Назначение:** Feature-Based модули приложения. Каждый модуль - это самодостаточная часть приложения со своими фичами, компонентами, хуками, утилитами и типами.

```
/modules
└── /habit-tracker      - Модуль трекера привычек
    ├── index.ts        - Public API модуля
    ├── types/          - Основные типы модуля
    ├── /features       - Фичи модуля (7 штук)
    │   ├── habits/
    │   ├── statistics/
    │   ├── calendar/
    │   ├── strength/
    │   ├── tags/
    │   ├── frequency/
    │   └── notifications/
    └── /shared         - Habit-specific переиспользуемые модули
        ├── components/
        ├── hooks/
        ├── constants/
        └── types/
```

### `/modules/habit-tracker` - Модуль трекера привычек

Полнофункциональный модуль для отслеживания привычек с календарной сеткой, статистикой и системой силы привычки.

**Корневые файлы:**
- **`index.ts`** - Public API модуля (экспорт типов и главных компонентов)
- **`types/index.ts`** - Основные типы модуля (Habit, HabitData, HabitType, FrequencyConfig, Reminder, MeasurableSettings)

---

### `/modules/habit-tracker/features` - Фичи модуля

Каждая фича - это самодостаточный модуль со своей папочной структурой: components, hooks, utils, types.

#### `habits/` - CRUD привычек

**Назначение:** Управление привычками (добавление, редактирование, удаление), отображение списка привычек, статистика привычки.

**Структура:**
```
/habits
├── index.ts            - Public API фичи
├── /components         - Компоненты привычек
│   ├── HabitRow.tsx
│   ├── HabitStatisticsModal.tsx
│   ├── HabitCheckboxCell.tsx
│   ├── HabitsListPanel.tsx
│   ├── HabitsTable.tsx
│   ├── AddHabitModal.tsx
│   ├── ManageHabitsModal.tsx
│   ├── NumericInputModal.tsx
│   ├── DeleteDialog.tsx
│   ├── /add            - Подкомпоненты добавления
│   └── /manage         - Подкомпоненты управления
├── /hooks              - Хуки привычек
│   ├── useHabitOperations.ts
│   └── useHabitState.ts
├── /types              - Типы UI компонентов
│   ├── filters.ts
│   ├── props.ts
│   └── modal.ts
└── /utils              - Утилиты привычек
    └── habitUtils.ts
```

**Компоненты:**
- **`HabitRow.tsx`** - Строка привычки в таблице с иконкой, названием, силой привычки
- **`HabitStatisticsModal.tsx`** - Модальное окно статистики одной привычки (график силы, детальная статистика)
- **`HabitCheckboxCell.tsx`** - Ячейка дня для отметки выполнения (бинарная/измеримая с прогрессом, оптимизирована через React.memo)
- **`HabitsListPanel.tsx`** - Панель списка привычек с кнопкой добавления
- **`HabitsTable.tsx`** - Основная таблица привычек с календарной сеткой
- **`AddHabitModal.tsx`** - Модальное окно добавления новой привычки (3 шага)
- **`ManageHabitsModal.tsx`** - Модальное окно управления существующими привычками
- **`NumericInputModal.tsx`** - Ввод числового значения для измеримых привычек
- **`DeleteDialog.tsx`** - Диалог подтверждения удаления привычки

**Подкомпоненты /add:**
- **`FrequencyModal.tsx`** - Вложенное модальное окно настройки частоты
- **`FrequencyModalTrigger.tsx`** - Кнопка-триггер для открытия модального окна частоты
- **`HabitBasicInfoStep.tsx`** - Шаг 1: Основная информация (название, иконка, тег) [min-h: 320px] [ОБНОВЛЕНО 23 ноября 2025 - миграция category → tag]
- **`HabitMeasurableStep.tsx`** - Шаг 2: Настройки измеримой привычки [min-h: 320px]
- **`HabitDetailsStep.tsx`** - Шаг 3: Дополнительные настройки (частота, напоминания) [min-h: 320px]
- **`HabitTypePicker.tsx`** - Выбор типа привычки (бинарная/измеримая) [МИГРИРОВАНО на Dropdown ✅]
- **`NotesSection.tsx`** - Секция заметок
- **`RemindersSection.tsx`** - Секция управления напоминаниями
- **`ModalFooter.tsx`** - Футер модального окна с кнопками навигации

**Подкомпоненты /manage:**
- **`ManageHabitsFooter.tsx`** - Футер с кнопками сохранения/отмены
- **`HabitNameEditor.tsx`** - Inline редактор названия
- **`HabitMeasurableSettingsSection.tsx`** - Настройки измеримой привычки
- **`HabitFrequencySection.tsx`** - Секция редактирования частоты
- **`HabitRemindersSection.tsx`** - Секция управления напоминаниями
- **`IconPicker.tsx`** - Выбор иконки с пагинацией [МИГРИРОВАНО на Dropdown ✅ 22 ноября 2025]
- **`UnitPicker.tsx`** - Выбор единицы измерения [МИГРИРОВАНО на Dropdown + группировка ✅]
- **`TargetTypePicker.tsx`** - Выбор типа цели (минимум/максимум) [МИГРИРОВАНО на Dropdown ✅]

**Хуки:**
- **`useHabitOperations.ts`** - Операции с привычками из store
- **`useHabitState.ts`** - Получение состояния привычек из store

**Утилиты:**
- **`habitUtils.ts`** - Проверка выполнения, получение значений, форматирование частоты

**Типы:**
- **`filters.ts`** - Типы фильтрации привычек
- **`props.ts`** - Типы пропсов компонентов (DateConfig, HabitActions, ModalActions)
- **`modal.ts`** - Типы модальных окон

---

#### `statistics/` - Статистика и прогресс

**Назначение:** Отображение статистики выполнения привычек (дневная, месячная), прогресс-бары, графики.

**Структура:**
```
/statistics
├── index.ts            - Public API фичи
└── /components         - Компоненты статистики
    ├── ProgressSection.tsx
    ├── DailyProgressBars.tsx
    ├── MonthlyCircle.tsx
    ├── MonthlyStats.tsx
    ├── StrengthProgressBar.tsx
    └── StrengthChart.tsx
```

**Компоненты:**
- **`ProgressSection.tsx`** - Основная секция прогресса привычек за месяц
- **`DailyProgressBars.tsx`** - Дневные полосы прогресса в заголовке календаря
- **`MonthlyCircle.tsx`** - Круговой индикатор месячного прогресса
- **`MonthlyStats.tsx`** - Детальная статистика за месяц с редактируемой целью
- **`StrengthProgressBar.tsx`** - Прогресс-бар силы привычки с градиентом
- **`StrengthChart.tsx`** - График изменения силы привычки с топ-10

---

#### `calendar/` - Календарь привычек

**Назначение:** Календарная сетка для отметки выполнения привычек, навигация по месяцам.

**Структура:**
```
/calendar
├── index.ts            - Public API фичи
└── /components         - Компоненты календаря
    ├── CalendarHeader.tsx
    ├── CalendarDayHeader.tsx
    ├── CalendarGrid.tsx
    ├── CircularProgress.tsx
    └── MonthYearPicker.tsx
```

**Компоненты:**
- **`CalendarHeader.tsx`** - Шапка календаря с названием месяца, навигацией и прогрессом
- **`CalendarDayHeader.tsx`** - Заголовок с днями недели и числами
- **`CalendarGrid.tsx`** - Основная календарная сетка с чекбоксами
- **`CircularProgress.tsx`** - Круговая диаграмма прогресса для измеримых привычек
- **`MonthYearPicker.tsx`** - Модальное окно выбора месяца и года

---

#### `strength/` - Сила привычки (EMA)

**Назначение:** Расчёт и отображение силы привычки на основе экспоненциальноо скользящего среднего (EMA).

**Структура:**
```
/strength
├── index.ts            - Public API фичи
├── /hooks              - Хуки силы привычки
   └── useStrengthUpdater.ts
└── /utils              - Утилиты расчёта силы
    ├── strengthCalculator.ts
    └── strengthHistory.ts
```

**Хуки:**
- **`useStrengthUpdater.ts`** - Автоматическое обновление силы привычек при загрузке приложения

**Утилиты:**
- **`strengthCalculator.ts`** - Пересчёт силы привычки с EMA алгоритмом и поддержкой "заморозки"
- **`strengthHistory.ts`** - Расчёт истории изменения силы для построения графиков

---

#### `tags/` - Теги привычек

**Назначение:** Управление тегами привычек (добавление, удаление, обновление цвета).

**Структура:**
```
/tags
├── index.ts            - Public API фичи
├── /components         - Компоненты тегов
│   └── HabitTagPicker.tsx
├── /types              - Типы тегов
│   └── index.ts        - Интерфейс Tag
└── /utils              - Утилиты тегов
    ├── tagHelpers.ts
    └── initializeHabitTags.ts
```

**Компоненты:**
- **`HabitTagPicker.tsx`** - Компонент выбора тега для привычки [СОЗДАН 23 ноября 2025]

**Типы:**
- **`Tag`** - Интерфйс тега (name, color)

**Утилиты:**
- **`tagHelpers.ts`** - Миграция legacy тегов, получение цвета тега по названию
- **`initializeHabitTags.ts`** - Инициализация дефолтных тегов с автоматической дедупликацией

---

#### `frequency/` - Частота выполнения

**Назначение:** Управление частотой выполнения привычек (ежедневно, N раз в неделю, по дням недели и т.д.).

**Структура:**
```
/frequency
├── index.ts            - Public API фичи
├── /components         - Компоненты настройки частоты
│   ├── FrequencyEditor.tsx
│   ├── FrequencyButton.tsx
│   ├── FrequencyInput.tsx
│   ├── DailyFrequencySection.tsx
│   ├── ByDaysOfWeekSection.tsx
│   ├── EveryNDaysSection.tsx
│   ├── NTimesWeekSection.tsx
│   ├── NTimesMonthSection.tsx
│   ├── NTimesInMDaysSection.tsx
│   └── SingleInputFrequencySection.tsx
├── /hooks              - Хуки частоты
│   └── useFrequencyState.ts
├── /types              - Типы частоты
│   └── index.ts
└── /utils              - Утилиты валидации
    └── frequencyValidation.ts
```

**Компоненты:**
- **`FrequencyEditor.tsx`** - Главный компонент редактора частоты с табами
- **`FrequencyButton.tsx`** - Кнопка выбора типа частоты
- **`FrequencyInput.tsx`** - Input поле для ввода чисел
- **`DailyFrequencySection.tsx`** - Секция "Ежедневно"
- **`ByDaysOfWeekSection.tsx`** - Секция "По дням недели"
- **`EveryNDaysSection.tsx`** - Секция "Каждые N дней"
- **`NTimesWeekSection.tsx`** - Секция "N раз в неделю"
- **`NTimesMonthSection.tsx`** - Секция "N раз в месяц"
- **`NTimesInMDaysSection.tsx`** - Секция "N раз за M дней"
- **`SingleInputFrequencySection.tsx`** - Универсальная секция с одним input

**Хуки:**
- **`useFrequencyState.ts`** - Управление локальным состоянием частоты

**Утилиты:**
- **`frequencyValidation.ts`** - Валидация count и period, лимиты

---

#### `notifications/` - Уведомления

**Назначение:** Управление уведомлениями о выполнении привычек.

**Структура:**
```
/notifications
├── index.ts            - Public API фичи
├── /components         - Компоненты уведомлений
│   └── HabitsNotificationManager.tsx
└── /services           - Habit-specific сервис уведомлений
    └── habitNotificationScheduler.ts
```

**Компоненты:**
- **`HabitsNotificationManager.tsx`** - Менеджер уведомлений для привычек (регистрация напоминаний)

**Сервисы:**
- **`habitNotificationScheduler.ts`** - Habit-specific обёртка над NotificationScheduler

---

### `/modules/habit-tracker/shared` - Habit-specific переиспользуемые модули

Модули, специфичные для habit-tracker, которые используются в нескольких фичах модуля.

**Структура:**
```
/shared
├── /components         - Habit-specific компоненты
│   └── /filters
│       └── HabitsFilterDropdown.tsx
├── /hooks              - Habit-specific хуки
│   └── /use-habits-filter
│       ├── useHabitsFilter.ts
│       └── index.ts
├── /constants          - Habit-specific константы
│   ├── strength.ts     - EMA_PERIOD, EMA_ALPHA
│   └── units.ts        - UNIT_OPTIONS (22 единицы измерения)
└── /types              - Habit-specific типы
    └── filters.ts
```

**Компоненты:**
- **`HabitsFilterDropdown.tsx`** - Выпадающий список фильтрации привычек по категориям

**Хуки:**
- **`useHabitsFilter.ts`** - Хук фильтрации списка привычек

**Константы:**
- **`strength.ts`** - Константы для расчёта силы привычки (период EMA, коэффициент альфа)
- **`units.ts`** - Список доступных единиц измерения для измеримых привычек

**Типы:**
- **`filters.ts`** - Типы фильтрации привычек

---

## 🔄 `/shared` - Универсальные переиспользуемые модули

**Назначение:** Модули общего назначения, используемые всеми модулями приложения. Код попадает сюда по правилу "трёх использований" - если модуль используется в трёх разных местах, он переносится в shared.

```
/shared
├── /components         - Универсальные UI компоненты
├── /hooks              - Универсальные React хуки
├── /utils              - Универсальные утилиты
├── /services           - Универсальные сервисы
├── /constants          - Глобальные константы
├── /constructors       - Композитные конструкторы
└── /icons              - Централизованный реэкспорт иконок
```

---

### `/shared/components` - Универсальные UI компоненты

```
/components
├── index.ts            - Barrel export всех компонентов
├── /button
│   ├── Button.tsx
│   └── index.ts
├── /layout
│   ├── Sidebar.tsx
│   ├── VersionIndicator.tsx
│   └── index.ts
├── /popovers
│   ├── /color-picker
│   │   ├── ColorPicker.tsx
│   │   └── index.ts
│   └── index.ts
├── /tag-picker
│   ├── TagPicker.tsx
│   ├── README.md       - 📚 Полная дкументация компонента
│   └── index.ts
├── /section-picker
│   ├── SectionPicker.tsx
│   ├── README.md       - 📚 Полная документация компонента
│   └── index.ts
├── /reminders
│   ├── RemindersManager.tsx
│   └── index.ts
├── /modals
│   ├── ConfirmDialog.tsx
│   ├── index.ts
│   └── README.md
└── /notifications
    ├── NotificationPermissionBanner.tsx
    └── index.ts
```

**Компоненты:**
- **`Button.tsx`** - Универсальный компонент кнопки (обёртка над shadcn)
- **`Sidebar.tsx`** - Боковая панель приложения с навигацией
- **`VersionIndicator.tsx`** - Индикатор версии приложения
- **`ColorPicker.tsx`** - Пикер цвета с пагинацией [МИГРИРОВАНО на Dropdown ✅ 22 ноября 2025]
- **`TagPicker.tsx`** - 🌟 **Reference Implementation** - Generic компонент выбора тегов с множественным выбором, защитой от дубликатов и keyboard navigation · [Документация](/shared/components/tag-picker/README.md) · Создан: 23 ноября 2025
- **`SectionPicker.tsx`** - 🌟 **Reference Implementation** - Generic компонент выбора разделов с множественным выбором, защитой от дубликатов и keyboard navigation · [Документация](/shared/components/section-picker/README.md) · Создан: 23 ноября 2025
- **`RemindersManager.tsx`** - Универсальный компонент управления напоминаниями
- **`ConfirmDialog.tsx`** - Универсальный диалог подтверждения
- **`NotificationPermissionBanner.tsx`** - Баннер запроса разрешений на уведомления

---

### `/shared/hooks` - Универсальные React хуки

```
/hooks
├── index.ts            - Barrel export всех хуков
├── /use-dropdown
│   ├── useDropdown.ts
│   ├── useDropdown.example.tsx
│   ├── index.ts
│   └─ README.md
└── /use-click-outside
    ├── useClickOutside.ts
    ├── index.ts
    └── README.md
```

**Хуки:**
- **`useDropdown.ts`** - Универсальный хук управления dropdown состоянием
- **`useClickOutside.ts`** - Хук обработки кликов вне элемента с поддержкой вложенных dropdown · Обновлено: 23 ноября 2025

---

### `/shared/utils` - Универсальные утилиты

```
/utils
├── README.md           - Общая документация утилит
├── /logger
│   ├── logger.ts
│   ├── index.ts
│   └── README.md
├── /date
│   ├── dateUtils.ts
│   └── index.ts
└── /text
    ├── textUtils.ts
    └── index.ts
```

**Утилиты:**
- **`logger.ts`** - Модульная система логирования с категориями и уровнями
- **`dateUtils.ts`** - Форматирование дат, получение дней в месяце, названий дней
- **`textUtils.ts`** - Склонение слов, форматирование чисел, единицы измерения

---

### `/shared/services` - Универсальные сервисы

```
/services
├── /notifications
│   ├── NotificationService.ts
│   ├── index.ts
│   └── README.md
└── /notification-scheduler
    ├── NotificationScheduler.ts
    ├── types.ts
    ├── index.ts
    └── README.md
```

**Сервисы:**
- **`NotificationService.ts`** - Низкоуровневый сервис Web Notifications API (статический класс) · Обновлено: 22 ноября 2025
- **`NotificationScheduler.ts`** - Централизованный планировщик уведомлений с умной группировкой

---

### `/shared/constants` - Глобальные константы

```
/constants
├── index.ts            - Централизованный экспорт всех констант
├── README.md           - Полная документация констант
├── colors.ts           - Цветовая палитра (категории, статусы, графики)
├── icons.ts            - Конфигурация доступных иконок
├── styles.ts           - Стили модальных окон, input полей, z-index
├── ui.ts               - UI константы (размеры, отступы, пагинация)
└── validation.ts       - Правила валидации и лимиты
```

**Константы:**
- **`colors.ts`** - Палитра цветов для тегов, статусов, графиков, UI элементов (20 базовых цветов Tailwind, исключая neutral и slate)
- **`icons.ts`** - Список доступных иконок для привычек с категориями
- **`styles.ts`** - Стили модальных окон, input полей, z-index слоёв, transitions
- **`ui.ts`** - Размеры, отступы, количество элементов на странице
- **`validation.ts`** - Лимиты длины полей, правила валидации

---

### `/shared/constructors` - Композитные конструкторы

```
/constructors
├── README.md           - Описание паттерна Constructor Pattern
├── /modal
│   ├── Modal.tsx       - Композитные компоненты модального окна
│   └── index.ts
└── /dropdown
    ├── Dropdown.tsx    - Композитные компоненты dropdown меню
    ├── README.md       - Документация и примеры использования
    ├── index.ts
    └── /examples       - Примеры использования
        ├── basic-examples.tsx       - Базовые примеры
        ├── search-example.tsx       - Поиск и фильтрация
        ├── grouping-example.tsx     - Группировка элементов
        └── responsive-maxheight-example.tsx - Адаптивная высота
```

**Конструкторы:**
- **`Modal.tsx`** - Универсальная система модальных окон (Root, Backdrop, Content, Header, Footer, CloseButton)
- **`Dropdown.tsx`** - Универсальная система dropdown меню (Root, Trigger, Content, Item, Separator, Group, Search, Empty)

---

### `/shared/icons` - Централизованный реэкспорт иконок

```
/icons
└── index.ts            - Реэкспорт всех иконок из lucide-react
```

**Файлы:**
- **`index.ts`** - Единая точка импорта всех иконок с алиасами

---

## 📦 `/components` - UI компоненты

**Назначение:** Защищённые компоненты Figma/shadcn. Эта папка содержит только базовые UI компоненты из библиотеки shadcn/ui и служебные компоненты Figma.

```
/components
├── /ui                 - Shadcn UI компоненты (защищено)
└── /figma              - Figma компоненты (защищено)
```

### `/components/ui` - Shadcn UI омпоненты ⚠️ **ЗАЩИЩЕНО**

**Статус:** Эти файлы НЕ переносятся и НЕ изменяются. Остаются в `/components/ui/`.

**Список компонентов:** accordion, alert-dialog, alert, aspect-ratio, avatar, badge, breadcrumb, button, calendar, card, carousel, chart, checkbox, collapsible, command, context-menu, dialog, drawer, dropdown-menu, form, hover-card, input-otp, input, label, menubar, navigation-menu, pagination, popover, progress, radio-group, resizable, scroll-area, select, separator, sheet, sidebar, skeleton, slider, sonner, switch, table, tabs, textarea, toggle-group, toggle, tooltip, use-mobile, utils.

### `/components/figma` - Figma компоненты ⚠️ **ЗЩИЩЕНО**

**Статус:** Эти файлы защищены системой Figma, НЕ трогаем.

**Файлы:**
- **`ImageWithFallback.tsx`** - Компонент зображения с fallback

---

## 📚 `/docs` - Докуметация

```
/docs
├── README.md              - Главная навигация по документации
├── FileStructure.md       - Этот файл (структура пректа)
├── HISTORY.md             - Хронология изменений (1-22 ноября 2025)
│
├── /getting-started       - 🚀 Начало работы
│   ├── README.md          - авигация по разделу
│   ├── COMMON_TASKS.md    - 5 пошаговых инструкций (общие задачи)
│   └── COMPONENT_PATTERNS.md - Паттерны кода, naming conventions
│
├── /architecture          - 🏗️ Архитектура
│   ├── README.md          - Навигация по разделу
│   ├── STORE_ARCHITECTURE.md - Архитектура Zustand Store
│   └── UI_SEPARATION_RULES.md - Правила разделения UI
│
├── /ui-systems            - 🎨 UI системы
│   ├── README.md          - Навигация по разделу
│   ├── MODAL_SYSTEM.md    - Универсальная система модальных окон
│   └── INPUT_STYLES_EXAMPLES.md - Примеры стилей input
│
├── /state-management      - 🏪 State Management
│   ├── README.md          - Навигация по разделу
│   └── ZUSTAND_QUICKSTART.md - Быстрая шпаргалка
│
├── /logging               - 🐛 Логирование и отладка
│   ├── README.md          - Навигация по разделу
│   ├── LOGGER_QUICKSTART.md - Быстрый старт
│   ├── LOGGER_USAGE.md    - Детальное использование
│   └── LOGGER_MODULE_FILTERING.md - Фильтрация логов
│
├── /meta                  - 📦 Метаинформация
│   ├── README.md          - Навигация по разделу
│   └── PROJECT_RULES.md   - Правила создания документов
│
└── /temp                  - 🚧 Временные документы (анализы, кандидаты)
```

**Документы в корне `/docs/`:**
- **`README.md`** - Главная навигация по всей документации с быстрыми ссылками
- **`FileStructure.md`** - Полная структура файлов проекта (этот файл)
- **`HISTORY.md`** - Детальная хронология всех изменений проекта (1-22 ноября 2025)

**Основные разделы:**
- **`getting-started/`** - Начало работы (COMMON_TASKS, COMPONENT_PATTERNS)
- **`architecture/`** - Архитектура (STORE_ARCHITECTURE, UI_SEPARATION_RULES)
- **`ui-systems/`** - UI системы (MODAL_SYSTEM, INPUT_STYLES_EXAMPLES)
- **`state-management/`** - State Management (ZUSTAND_QUICKSTART)
- **`logging/`** - Логирование (LOGGER_QUICKSTART, LOGGER_USAGE, LOGGER_MODULE_FILTERING)
- **`meta/`** - Метаинформация (PROJECT_RULES)
- **`STORE_ARCHITECTURE.md`** - Детальное описание архитектуры Zustand Store
- **`COMPONENT_PATTERNS.md`** - Паттерны кода, naming conventions, best practices

**Системы:**
- **`MODAL_SYSTEM.md`** - Документация универсальной системы модальных окон

**Zustand:**
- **`ZUSTAND_QUICKSTART.md`** - Краткая шпаргалка по работе со store

**Логирование:**
- **`LOGGER_QUICKSTART.md`** - Быстрый старт с системой логирования
- **`LOGGER_USAGE.md`** - Детальное руководство по использованию logger
- **`LOGGER_MODULE_FILTERING.md`** - Фильтрация логов по модулям

**Стили:**
- **`INPUT_STYLES_EXAMPLES.md`** - Примеры стилизации input полей

**Метаинформация:**
- **`PROJECT_RULES.md`** - Правила создания документов и ответов в чате

> 📝 **Примечание:** Информация о рефакторингах, оптимизации, багфиксах и миграциях хранится в **HISTORY.md**. Отдельные документы были архивированы.