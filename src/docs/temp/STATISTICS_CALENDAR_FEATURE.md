# 📅 Календарь в модальном окне статистики

**Дата создания:** 24 ноября 2025  
**Статус:** 🚧 В планировании

---

## 🎯 Цель

Добавить справа от блока "Сила привычки" (прогресс-бар + график) календарь с дублированием галочек из основного интерфейса.

## 📸 Референс

Скриншот от конкурентов показывает:
- Календарь справа от графика статистики
- Синие круглые отметки на выполненных днях
- Диапазоны дней (10-14) объединены в прямоугольник
- Стрелка на текущем дне (22)
- Возможность переключения месяцев (но нельзя выбрать будущие)

---

## ❓ Вопросы для уточнения

### 1. **Визуальное отображение дней**

**Вопрос:** Как отображать разные типы дней в календаре?

**Варианты:**
- ✅ **Выполненные дни (✓):**
  - Синий круг/фон (как на скриншоте)?
  - Какой оттенок синего? `bg-blue-500`?
  
- ✗ **Замороженные дни (✗ крестик):**
  - Серый круг/фон?
  - Или специальная иконка крестика внутри круга?
  
- ○ **Дни без отметок:**
  - Обычный текст без фона?
  - Или легкий border?
  
- 🔒 **Будущие дни:**
  - Серый/неактивный текст (`text-gray-400`)?
  - Или вообще не показывать?

---

### 2. **Измеримые привычки**

**Вопрос:** Как показывать выполнение для привычек с числовым значением?

**Варианты:**
- **Вариант A (простой):** Синий круг если выполнено хотя бы частично (>0)
- **Вариант B (градация):** Цвет зависит от процента выполнения:
  - 0-25% — `bg-blue-200` (светло-синий)
  - 25-50% — `bg-blue-300`
  - 50-75% — `bg-blue-400`
  - 75-100% — `bg-blue-500` (ярко-синий)
- **Вариант C (число):** Показывать число внутри круга (может быть слишком много информации)

**Рекомендация:** Вариант B (градация) — визуально понятно и информативно

---

### 3. **Индикатор текущего дня**

**Вопрос:** Нужна ли стрелка/индикатор на текущем дне?

**Варианты:**
- Стрелка сверху (как на скриншоне)
- Border вокруг числа (`ring-2 ring-orange-500`)
- Жирный шрифт + цвет (`font-bold text-orange-500`)

**Рекомендация:** Ring вокруг числа — минималистичнее и в стиле Jony Ive

---

### 4. **Диапазоны дней**

**Вопрос:** Объединять ли последовательные выполненные дни в диапазоны (прямоугольник)?

**Варианты:**
- **Вариант A (диапазоны):** 10-14 → один синий прямоугольник
  - ➕ Визуально показывает серии (стрики)
  - ➖ Сложнее реализация
  
- **Вариант B (отдельные круги):** Каждый день = отдельный круг
  - ➕ Проще реализация
  - ➕ Понятнее для пользователя (кликабельность?)
  - ➖ Менее наглядно для стриков

**Рекомендация:** Начать с Варианта B (отдельные круги), потом можно добавить диапазоны

---

### 5. **Начальный месяц**

**Вопрос:** На каком месяце должен открываться календарь?

**Варианты:**
- **Вариант A:** Всегда текущий месяц
- **Вариант B:** Месяц, выбранный в основном интерфейсе (`monthYearKey` из props)

**Рекомендация:** Вариант B — более логично и связано с основным интерфейсом

---

### 6. **Ограничения навигации**

**Требование:** Нельзя выбрать будущий месяц, максимум текущий. Предыдущие выбирать можно.

**Реализация:**
```typescript
// Получаем текущую дату
const today = new Date();
const currentMonthKey = `${today.getFullYear()}-${String(today.getMonth() + 1).padStart(2, '0')}`;

// Проверка при переключении месяца
const canGoNext = selectedMonthKey < currentMonthKey;
const canGoPrev = true; // Всегда можно в прошлое

// Кнопка "Следующий месяц" disabled если уже текущий
<button disabled={!canGoNext}>→</button>
```

---

## 🏗️ Архитектурное решение

### Компоненты

```
HabitStatisticsModal.tsx
└─ StatisticsCalendar.tsx (новый компонент)
   ├─ CalendarHeader.tsx (месяц + навигация)
   ├─ CalendarGrid.tsx (сетка 7x5/6)
   └─ CalendarDay.tsx (отдельная ячейка дня)
```

### Структура файлов

```
/modules/habit-tracker/features/habits/components/
├─ HabitStatisticsModal.tsx (изменить)
└─ statistics-calendar/
   ├─ StatisticsCalendar.tsx
   ├─ CalendarHeader.tsx
   ├─ CalendarGrid.tsx
   ├─ CalendarDay.tsx
   ├─ types.ts
   ├─ utils.ts
   └─ index.ts
```

---

## 🎨 Layout в HabitStatisticsModal

### Текущая структура:
```
[Сила привычки] <- полная ширина
[Прогресс-бар]
[График]
```

### Новая структура (2 колонки):
```
[Сила привычки + График]  |  [Календарь]
       (70% ширины)        |   (30% ширины)
```

**Реализация:**
```tsx
<div className="flex gap-6">
  {/* Левая колонка: График */}
  <div className="flex-[2]"> {/* 66% ширины */}
    <StrengthProgressBar />
    <LineChart />
  </div>
  
  {/* Правая колонка: Календарь */}
  <div className="flex-[1]"> {/* 33% ширины */}
    <StatisticsCalendar />
  </div>
</div>
```

---

## 📊 Данные

### Props для StatisticsCalendar:

```typescript
interface StatisticsCalendarProps {
  habit: Habit;
  initialMonth?: string; // "2025-11" из monthYearKey
}
```

### Логика:

```typescript
// Получить все даты с галочками для выбранного месяца
const completions = habit.completions; // { "2025-11-15": true, ... }
const skipped = habit.skipped; // { "2025-11-20": true, ... }

// Для измеримых привычек
const measurableValues = habit.completions; // { "2025-11-15": 5000, ... }
const dailyGoal = habit.measurableConfig?.dailyGoal; // 10000

// Рассчитать процент выполнения
const percentage = (measurableValues[date] / dailyGoal) * 100;
```

---

## ✅ Рекомендуемый план реализации

### Этап 1: Базовый календарь (30 минут)
1. Создать `StatisticsCalendar.tsx` с сеткой дней
2. Добавить навигацию по месяц��м с ограничениями
3. Отобразить числа дней

### Этап 2: Визуализация галочек (20 минут)
4. Для binary привычек: синий круг на выполненных днях
5. Для skipped дней: серый круг
6. Будущие дни: неактивный вид

### Этап 3: Измеримые привычки (15 минут)
7. Градация цвета по проценту выполнения

### Этап 4: Полировка (10 минут)
8. Индикатор текущего дня (ring)
9. Hover эффекты
10. Responsive для узких экранов

### Этап 5 (опционально): Диапазоны
11. Алгоритм объединения последовательных дней
12. Рендеринг прямоугольников вместо кругов

---

## 🎯 Минимально жизнеспособный продукт (MVP)

**Что точно нужно:**
- ✅ Календарь справа от графика
- ✅ Навигация по месяцам (с ограничением на будущее)
- ✅ Синие круги на выполненных днях
- ✅ Серые на замороженных
- ✅ Неактивный вид будущих дней

**Что можно отложить:**
- ⏳ Диапазоны (прямоугольники)
- ⏳ Градация цвета для измеримых
- ⏳ Стрелка на текущем дне

---

## 💡 Дополнительные идеи

### 1. Tooltip при наведении
При наведении на день показывать детали:
- Binary: "Выполнено" / "Пропущено" / "Заморожено"
- Measurable: "5000 / 10000 шагов (50%)"

### 2. Клик на день
Возможность открыть детали дня или быстро изменить статус?
- Опасно: можно случайно изменить историю
- Лучше: только просмотр, без редактирования

### 3. Легенда
Добавить легенду под календарем:
```
🟦 Выполнено  ⚪ Заморожено  ⚫ Пропущено
```

---

## 🚀 Готов к реализации

Как только получим ответы на вопросы 1-5, можно начинать разработку!

**Примерное время:** 1-1.5 часа на полную реализацию MVP
