# 🎯 Кандидаты на композитные конструкторы

> **Статус:** ⏳ TODO (возврат после миграции Button)  
> **Дата создания:** 21 ноября 2025

---

## 📋 Список кандидатов

### 🔥 Высокий приоритет

#### 1. **Dropdown** ⏳
- **Проблема:** Дублирование логики открытия/закрытия, позиционирования
- **Примеры:** CategoryPicker, MonthYearPicker
- **Композитная структура:**
  - `Dropdown.Root` — корневой контейнер
  - `Dropdown.Trigger` — кнопка открытия
  - `Dropdown.Content` — содержимое выпадашки
  - `Dropdown.Item` — элемент списка
- **Статус:** ⏳ TODO

---

#### 2. **StatCard** ⏳
- **Проблема:** Повторяющиеся карточки статистики
- **Примеры:** MonthlyStats, HabitStatisticsModal
- **Композитная структура:**
  - `StatCard.Root` — корневая карточка
  - `StatCard.Title` — заголовок
  - `StatCard.Value` — основное значение
  - `StatCard.Description` — описание/подпись
  - `StatCard.Icon` — иконка
- **Статус:** ⏳ TODO

---

#### 3. **FormField** ⏳
- **Проблема:** Дублирование структуры label + input + error
- **Примеры:** AddHabitModal, CategoryPicker
- **Композитная структура:**
  - `FormField.Root` — обёртка поля
  - `FormField.Label` — подпись
  - `FormField.Input` — поле ввода
  - `FormField.Error` — сообщение об ошибке
  - `FormField.Hint` — подсказка
- **Статус:** ⏳ TODO

---

### ⚡ Средний приоритет

#### 4. **Button** 🔄
- **Проблема:** Нет кнопок-иконок, группировки
- **Примеры:** MonthlyStats (строка 230-238), ModalFooter
- **Композитная структура:**
  - `Button.Root` — обычная кнопка (существующая)
  - `Button.Icon` — кнопка-иконка (квадратная)
  - `Button.Group` — группа кнопок
- **Статус:** 🔄 В РАБОТЕ (обсуждается гибридный подход)

---

#### 5. **Card** ⏳
- **Проблема:** Повторяющаяся структура карточек
- **Примеры:** HabitRow, CategoryCard
- **Композитная структура:**
  - `Card.Root` — корневая карточка
  - `Card.Header` — шапка
  - `Card.Body` — тело
  - `Card.Footer` — подвал
- **Статус:** ⏳ TODO

---

### 🔹 Низкий приоритет

#### 6. **EmptyState** ⏳
- **Проблема:** Дублирование пустых состояний
- **Примеры:** "Нет привычек", "Нет данных"
- **Композитная структура:**
  - `EmptyState.Root` — контейнер
  - `EmptyState.Icon` — иконка
  - `EmptyState.Title` — заголовок
  - `EmptyState.Description` — описание
  - `EmptyState.Action` — кнопка действия
- **Статус:** ⏳ TODO

---

#### 7. **Badge** ⏳
- **Проблема:** Различные стили бейджей (категории, статусы)
- **Примеры:** Category цвета, статусы привычек
- **Композитная структура:**
  - `Badge.Root` — базовый бейдж
  - `Badge.Dot` — бейдж с точкой
  - `Badge.Icon` — бейдж с иконкой
- **Статус:** ⏳ TODO

---

## 🎯 План действий

1. ✅ Завершить миграцию Modal (уже сделано)
2. 🔄 Решить вопрос с Button (гибридный подход)
3. ⏳ Dropdown — первый кандидат после Button
4. ⏳ StatCard — второй кандидат
5. ⏳ FormField — третий кандидат
6. ⏳ Остальные по необходимости

---

## 📊 Критерии приоритизации

**Высокий приоритет:**
- Много дублирования
- Используется в 5+ компонентах
- Сложная логика (состояние, позиционирование)

**Средний приоритет:**
- Умеренное дублирование
- Используется в 3-5 компонентах
- Простая структура

**Низкий приоритет:**
- Минимальное дублирование
- Используется в 1-3 компонентах
- Очень простая структура

---

## 💡 Примечания

- Каждый конструктор должен следовать паттерну из `Modal.tsx`
- Обратная совместимость приветствуется (как в Button)
- Документация обязательна (`README.md` в папке компонента)
- Обновление `FileStructure.md` после создания

---

**Последнее обновление:** 21 ноября 2025
