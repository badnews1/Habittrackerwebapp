# 🎯 /core - Ядро приложения

> **Дата создания:** 21 ноября 2025  
> **Последнее обновление:** 22 ноября 2025  
> **Назначение:** Централизованное хранилище состояния и менеджеры модальных окон

---

## 📋 Содержание

- **`/store/`** - Zustand store с централизованным состоянием
- **`/modals/`** - Менеджеры модальных окон для разных модулей

---

## 📁 Структура

```
/core
  /store
    index.ts           - Главный store (объединение всех slices)
    types.ts           - Глобальные типы store
    initialState.ts    - Начальное состояние
    /slices
      addHabitForm.ts
      categories.ts
      goals.ts
      habits.ts
      internal.ts
      manageHabitsModal.ts
      modals.ts
      ui.ts
  /modals
    HabitTrackerModals.tsx  - Менеджер модальных окон habit-tracker
    index.ts                - Barrel export
```

---

## 🎯 Назначение

### `/store/`
- **Zustand store** с централизованным состоянием приложения
- Архитектура на базе **слайсов** (slices pattern)
- Middleware: persist (localStorage), devtools

### Слайсы (Slices):
1. **habits** - CRUD привычек, completion, force calculations
2. **categories** - CRUD категорий, дефолтные категории
3. **addHabitForm** - Состояние формы добавления привычки (3 шага)
4. **manageHabitsModal** - Состояние модалки управления привычками
5. **modals** - Открытие/закрытие всех модалок
6. **ui** - UI состояние (currentMonth, currentYear)
7. **goals** - Цели и прогресс
8. **internal** - Внутренняя логика (hydration, logging)

---

## 📝 Примеры использования

### Использование store в компонентах:

```typescript
import { useHabitsStore } from '@/core/store';
import { useShallow } from 'zustand/react/shallow';

function MyComponent() {
  // ✅ Оптимизированный селектор (useShallow)
  const { habits, addHabit, deleteHabit } = useHabitsStore(
    useShallow((state) => ({
      habits: state.habits,
      addHabit: state.addHabit,
      deleteHabit: state.deleteHabit,
    }))
  );

  // Использование...
}
```

---

## 🔄 История миграции

### 21 ноября 2025 - Миграция в /core/store/
- ✅ Создана структура `/core/store/`
- ✅ Перенесён весь store из старого расположения
- ✅ Обновлены импорты на `@/core/store`
- ✅ Миграция завершена полностью

---

## ⚠️ Важные заметки

- **НЕ используйте store напрямую** - только через хуки `useHabitsStore`
- **Используйте `useShallow`** для множественных селекторов (оптимизация)
- **Не создавайте новые глобальные слайсы** - новая логика должна быть в `/features/`

---

**Обновлено:** 22 ноября 2025  
**Статус:** ✅ Готово к использованию