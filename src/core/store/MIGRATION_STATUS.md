# ✅ Статус миграции /core/store/

> **Дата:** 21 ноября 2025  
> **Этап:** Этап 2 - Создание /core/

---

## 📊 Прогресс

### ✅ Готово (5 файлов):
1. ✅ `/core/store/index.ts` - главный store
2. ✅ `/core/store/types.ts` - типы
3. ✅ `/core/store/initialState.ts` - начальное состояние
4. ✅ `/core/store/slices/ui.ts` - UI slice
5. ✅ `/core/store/slices/modals.ts` - Modals slice
6. ✅ `/core/store/slices/addHabitForm.ts` - AddHabitForm slice

### ⏳ Осталось скопировать (5 слайсов):
1. ⏳ `/core/store/slices/habits.ts` - CRUD привычек (критично!)
2. ⏳ `/core/store/slices/categories.ts` - Категории
3. ⏳ `/core/store/slices/goals.ts` - Цели
4. ⏳ `/core/store/slices/manageHabitsModal.ts` - Модалка управления
5. ⏳ `/core/store/slices/internal.ts` - Внутренняя логика

---

## 📝 Следующие шаги

### 1. Завершить копирование слайсов (10 минут)
```bash
# Скопировать оставшиеся 5 файлов из /stores/habitsStore/slices/ в /core/store/slices/
cp /stores/habitsStore/slices/habits.ts /core/store/slices/habits.ts
cp /stores/habitsStore/slices/categories.ts /core/store/slices/categories.ts
cp /stores/habitsStore/slices/goals.ts /core/store/slices/goals.ts
cp /stores/habitsStore/slices/manageHabitsModal.ts /core/store/slices/manageHabitsModal.ts
cp /stores/habitsStore/slices/internal.ts /core/store/slices/internal.ts
```

### 2. Обновить импорты (20-30 минут)
Файлы с импортами store:
- `/App.tsx` - главный файл
- Все компоненты привычек
- Все модальные окна

Обновить импорты:
```typescript
// ❌ Старый путь
import { useHabitsStore } from './stores/habitsStore';

// ✅ Новый путь
import { useHabitsStore } from '@/core/store';
```

### 3. Проверка работоспособности (5 минут)
- ✅ Приложение запускается
- ✅ Добавление привычки работает
- ✅ Модалки открываются
- ✅ Store сохраняется в localStorage

### 4. Удалить старые файлы (после проверки)
```bash
# Удалить /stores/habitsStore/ целиком
rm -rf /stores/habitsStore/
```

### 5. Обновить документацию
- Обновить `/docs/FileStructure.md`
- Обновить `/docs/MIGRATION_LOG.md`
- Добавить запись в `/docs/HISTORY.md`

---

## ⚠️ Важные заметки

- **НЕ удаляй старые файлы** до полной проверки работоспособности!
- **Временные алиасы** в `tsconfig.json` работают - старые импорты не сломаются сразу
- **После копирования** всех слайсов - обнови импорты в комментариях `@module`
- **Store работает** через алиасы - можно тестировать постепенно

---

## 🎯 Цель Этапа 2

Вынести Zustand store в `/core/store/` для лучшей организации архитектуры.

**Преимущества:**
- ✅ Централизованное управление состоянием
- ✅ Чёткое разделение: `/core/` (состояние) vs `/features/` (фичи) vs `/shared/` (общее)
- ✅ Подготовка к масштабированию

---

**Обновлено:** 21 ноября 2025  
**Статус:** 🚧 60% готово (6 из 11 файлов)
